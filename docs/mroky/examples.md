# mroky Examples

Examples will live here as the implementation develops.

## Minimal application

The intended API will eventually look similar to:

```cpp
import mroky;

int main() {
    // create a node
    // discover peers
    // open a session
    // exchange messages
}
```

The API shown above is illustrative and is not currently a stable API.
